# docker/

Empty for now by design. `docker-compose.yml` at the repo root already runs
PostgreSQL for local development (Section 15 of the implementation guide,
Day 1). Backend/frontend Dockerfiles belong here once the backend is stable
enough to containerize meaningfully — planned for Sprint 2–3, not before.
